(ns org.archimitra.vertx.block-eveny-loop.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.vertx.block-eveny-loop.interface :as block-eveny-loop]))

(deftest dummy-test
  (is (= 1 1)))
