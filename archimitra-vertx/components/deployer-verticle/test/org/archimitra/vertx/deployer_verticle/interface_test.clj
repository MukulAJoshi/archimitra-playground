(ns org.archimitra.vertx.deployer-verticle.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.vertx.deployer-verticle.interface :as deployer-verticle]))

(deftest dummy-test
  (is (= 1 1)))
