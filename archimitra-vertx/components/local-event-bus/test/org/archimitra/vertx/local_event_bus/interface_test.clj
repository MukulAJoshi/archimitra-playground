(ns org.archimitra.vertx.local-event-bus.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.vertx.local-event-bus.interface :as local-event-bus]))

(deftest dummy-test
  (is (= 1 1)))
