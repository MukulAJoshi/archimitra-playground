(ns org.archimitra.vertx.worker-verticle.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.vertx.worker-verticle.interface :as worker-verticle]))

(deftest dummy-test
  (is (= 1 1)))
