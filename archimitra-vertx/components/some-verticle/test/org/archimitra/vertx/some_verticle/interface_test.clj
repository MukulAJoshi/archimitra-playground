(ns org.archimitra.vertx.some-verticle.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.vertx.some-verticle.interface :as some-verticle]))

(deftest dummy-test
  (is (= 1 1)))
