(ns org.archimitra.vertx.hello-verticle.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.vertx.hello-verticle.interface :as hello-verticle]))

(deftest dummy-test
  (is (= 1 1)))
