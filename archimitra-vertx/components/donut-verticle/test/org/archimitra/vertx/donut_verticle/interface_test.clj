(ns org.archimitra.vertx.donut-verticle.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.vertx.donut-verticle.interface :as donut-verticle]))

(deftest dummy-test
  (is (= 1 1)))
