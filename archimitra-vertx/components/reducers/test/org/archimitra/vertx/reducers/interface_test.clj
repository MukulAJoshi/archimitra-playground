(ns org.archimitra.vertx.reducers.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.vertx.reducers.interface :as reducers]))

(deftest dummy-test
  (is (= 1 1)))
