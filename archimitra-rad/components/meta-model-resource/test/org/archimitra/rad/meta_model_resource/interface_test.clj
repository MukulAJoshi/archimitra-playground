(ns org.archimitra.rad.meta-model-resource.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.rad.meta-model-resource.interface :as meta-model-resource]))

(deftest dummy-test
  (is (= 1 1)))
