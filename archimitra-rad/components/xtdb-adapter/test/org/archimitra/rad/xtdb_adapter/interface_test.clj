(ns org.archimitra.rad.xtdb-adapter.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.archimitra.rad.xtdb-adapter.interface :as xtdb-adapter]))

(deftest dummy-test
  (is (= 1 1)))
